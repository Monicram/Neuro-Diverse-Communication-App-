import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.neurodiverse.app',
  appName: 'NeuroDiverse Communication  App',
  webDir: 'dist'
};

export default config;
